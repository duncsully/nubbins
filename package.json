{
  "name": "nubbins",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "prepublish": "turbo run build lint",
    "publish": "changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "prettier": "latest",
    "tsup": "^6.4.0",
    "turbo": "latest"
  }
}
